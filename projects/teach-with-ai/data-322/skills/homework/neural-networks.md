---
name: neural-networks
description: Explain MLP architecture choices; debug a non-converging training loop; compare neural nets to classical ML for tabular data
---

# Homework Skill: Neural Networks

## TODO

MLP architecture intuition: layers as learned transformations, activation functions (ReLU, sigmoid, softmax at output). Backpropagation conceptually: gradients flow backward, weights update. Keras/PyTorch quickstart patterns for tabular classification. Common training failures: learning rate too high (loss explodes or oscillates), learning rate too low (loss stagnates), batch normalization as a stabilizer. When neural networks beat classical ML: large data, complex feature interactions, non-tabular inputs. When random forests win on tabular data: smaller datasets, need for interpretability, faster iteration.
