---
name: llms-as-tools
description: Review a student's LLM prompt for a data task; suggest improvements; explain LLM failure modes in data workflows
---

# Homework Skill: LLMs as Tools

## TODO

What LLMs are mechanically: token prediction, context windows, why they hallucinate API calls and library versions. Prompting patterns for data tasks: chain-of-thought for multi-step reasoning, few-shot for format control, structured output for JSON/code extraction. Reviewing a student's prompt: is the task specified precisely? Is the expected output format clear? Is there ambiguity that will cause an unhelpful response? LLMs for data wrangling (entity extraction, text normalization) vs. LLMs for code generation (always verify against actual documentation). RAG conceptual overview for course context.
